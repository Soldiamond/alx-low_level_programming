#!/bin/bash
#include <stdio.h>
/**
 * main - entry point
 *
 * Return: Always 0 (sucess)
 */
int main(void)
{
	char myString[] = "\"Programming is like building a multilingual puzzle";

	puts(myString);
	return (0)
}
